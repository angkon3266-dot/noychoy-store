# Roadmap status — Meridian Éclat storefront

*Tracker for the review dossier of **22 August 2026** ("The storefront is React
now. Here is everything else I found." — 119 findings, 8 reviewers).*

**Source dossier:** <https://claude.ai/code/artifact/f6acf911-295f-4b77-8320-ce7fba20864b>

---

## How to read and maintain this file

This is the **single place that says what is done**. The dossier is a snapshot
and goes stale the moment something ships; this file is re-verified against the
code.

- Every status below was re-checked against the repository on **2026-08-22**,
  after commit `7daefff`, by a fan-out audit whose claims were then
  adversarially re-verified. Where the dossier and the code disagreed, **the
  code won** — those rows are marked *(dossier stale)*.
- Status vocabulary:
  - **Done** — shipped, wired, reachable by a real user.
  - **Partial** — the mechanism exists but is unwired, unused, or only covers
    half the finding. The *what remains* column is the actual work.
  - **Todo** — nothing exists yet.
- **Owner** says who can close it:
  - `code` — a developer change in this repo.
  - `setting` — the store owner flips it in the admin panel. **No deploy.**
  - `content` — the store owner writes words.
  - `infra` — cPanel / server / DNS.
- When you finish something, change the status **and** add a line to the
  changelog at the bottom with the commit sha. Do not delete rows — a row that
  reads *Done* with a date is the evidence.

> **Read production from the database, not the browser.** LiteSpeed serves
> cached HTML until it is flushed in cPanel, so a page can show a setting that
> was changed long ago. One row in this file was wrong for exactly that reason.
> To check a live setting, ssh in and read it: `php artisan tinker --execute="…"`.

> Two caveats that apply to the whole file. Production's `.env` is gitignored,
> so any claim about live environment values is inferred from `DEPLOY.md` and
> must be confirmed on the server. And `deploy.sh` cannot flush the LiteSpeed
> page cache or reset the lsphp OPcache — both are cPanel steps after every
> deploy.

---

## Owner-only — nothing in this repo can do these

| # | Action | Where | Why it is blocking |
|---|---|---|---|
| 1 | **Flush LiteSpeed page cache** | cPanel to LiteSpeed Web Cache Manager to Flush All | Visitors are still being served the pre-React homepage intermittently |
| 2 | **Reset lsphp OPcache** | cPanel to MultiPHP Manager, toggle the PHP version away and back | PHP changes are not live for everyone until this runs |
| 3 | **Decide the free-delivery promise** | Admin to Settings (see step 01) | The banner promises free delivery over ৳3,000; checkout does not honour it. This changes your margins, so it is your call — not a bug to be fixed by guessing |
| 4 | **Rename the social handles** | Facebook / Instagram / Messenger accounts | The links themselves are admin settings, but the *accounts* are `Noychoylove` / `_noychoy_` and only you can rename them |
| 5 | **Unblock the AI crawlers** | Cloudflare to the `noychoy.com` zone to AI Crawl Control / Managed robots.txt | Cloudflare injects a managed `robots.txt` ahead of the app's own and blocks `GPTBot`, `ClaudeBot`, `Google-Extended`, `meta-externalagent` and others from the whole site. Nobody here chose that. It also keeps the shop out of AI Overviews. See `docs/SEO.md` §3.1 |
| 6 | **Create the Search Console + Bing properties** | search.google.com/search-console, bing.com/webmasters | Neither exists, so nothing measures whether any of the SEO work landed, and the sitemap has never been submitted. `docs/SEO.md` §3.2 |
| 7 | **Merge the duplicate categories, delete `dummy`** | Admin to Categories | `bracelet` vs `bracelet-2`, and `women` vs `women-jewelry` vs `jewellery-for-her`, are three pages competing for one intent — Google splits the signal and ranks none of them. `dummy` is a placeholder currently in the sitemap. `docs/SEO.md` §3.3 |
| 8 | **Decide the return-fee policy** | `SEO_RETURN_FEES` in `.env`, after deciding | The refund policy only accepts damaged/wrong items, so the schema omits `returnFees` rather than assert a policy the page does not state |
| 9 | **Re-register the Steadfast webhook** | steadfast.com.bd → Webhook → `https://noychoy.com/webhooks/steadfast` | Since the 2026-09-07 move the old `meridianeclat.shop` URL still answers (it is exempt from the redirect), but delivery-status pushes should land on the live domain before that exemption is retired |
| 10 | **Upload the NoyChoy logo and watermark** | Admin → Appearance (logo, mobile logo, favicon) and Admin → Media → Watermark | The rebrand cleared the Meridian Éclat wordmark, so the header shows the store name as text and the favicon is a placeholder "N"; the watermark image is still the old wordmark |
| 11 | ~~Paste the OpenAI key and switch the assistant on~~ | Done by the owner on 2026-09-08 | The assistant is live on noychoy.com |
| 12 | **Tell Google and Meta about the move** | Search Console → Change of address (`meridianeclat.shop` → `noychoy.com`), then resubmit `https://noychoy.com/sitemap.xml`; Meta Business → Brand Safety → Domains, verify `noychoy.com` | The 301s carry the ranking signal, but the tooling has to be told; `docs/SEO.md` §3.2 |
| 13 | **Allow-list `noychoy.com` on the Meta dataset, and set a Test Event Code** | Events Manager → dataset `NoyChoy Meta pixel 01` → Diagnostics → "Confirm domain" → add `noychoy.com`; Events Manager → Test events → copy the code into Admin → Meta → Tracking | Meta flagged "all Purchase events send the same price" because the admin Test panel used to send ৳1 sample purchases into the real dataset whenever no test code was set. The panel now refuses without a code (2026-09-08); the warning ages out on its own once a few days of real orders have passed |
| 14 | **Review the new automations in Admin → Offers** | Admin → Offers: referral points (default 300), the "Birthday & anniversary messages" card (on by default, incl. SMS; occasion offer percent defaults to 0, i.e. wishes without a coupon) | Shipped 2026-09-08 with sensible defaults, but the SMS cost and the offer size are the owner's call |
| 15 | **Decide how far the assistant may go on its own** | Admin → System Config → AI assistant: order taking on/off, the per-customer daily cap (3) and the biggest order it may place unaided (৳20,000) | It now places real cash-on-delivery orders. Those three numbers are the whole of your exposure to it getting something wrong, and only you can set them |

---

## Step 01 — Settle the free-delivery promise

**Roadmap:** *"Flush the caches, then settle the free-delivery promise."*

| Item | Status | Owner | What remains |
|---|---|---|---|
| Checkout honours one server-side verdict | **Done** | code | `CartService::hasFreeShipping()` is the single authority; cart progress bar, checkout badge, zone picker and order total all read it (`7daefff`) |
| Threshold settable without SSH | **Done** | code | Was env-only (`FREE_SHIPPING_THRESHOLD`); now a field in Admin → Settings beside the shipping rates |
| Banner cannot drift from checkout | **Done** | code | Announcement messages accept a `{free_delivery}` placeholder that prints the live threshold, and the whole message is **dropped** while the promise is off. A migration rewrote the stored banner's literal `৳3000` into the placeholder — conservatively: only a message that mentions free delivery *and* contains exactly one amount |
| Threshold has regression tests | **Done** | code | `tests/Feature/FreeDeliveryPromiseTest.php` |
| **Set the actual number** | **Todo** | setting | Admin → Settings → *Free delivery over*. Leave blank to disable; the banner line then disappears rather than lying. Verified in a browser both ways: at ৳5,550 the zone picker is replaced by “Free delivery unlocked” and shipping is ৳0; with the field blank the picker returns, shipping is ৳130, and the banner stops mentioning free delivery |

---

## Step 02 — Fix the brand leaks

**Roadmap:** *"Social handles, footer wording, the header's Track Orders button,
and the default copy."* The dossier called this "all admin settings — no code".
That is **half right**: the live wrong values are settings, but the shipped
defaults, the emoji icon system and the palette are code.

| Item | Status | Owner | What remains |
|---|---|---|---|
| Social URLs + footer legal line | **Todo** | setting | Not hardcoded anywhere — the `config/theme.php` defaults are `null`. `Noychoylove` etc. are rows in the production `settings` table. Admin → Appearance → Footer, and → Conversion for Messenger |
| Repo-level brand leaks | **Done** | code | `.env.example` shipped `APP_NAME=Noychoy` and a typo'd `hello@nocyhoy.com`; the seeder created `admin@noychoy.com`; `app.css` called it the "Noychoy jewelry palette". All corrected. *Reversed on 2026-09-07:* the store is **NoyChoy** at `noychoy.com` now, so `APP_NAME="NoyChoy"` and `admin@noychoy.com` are back by design and `BrandChromeTest` guards against `Meridian Éclat` / `meridianeclat` instead |
| `footer_about` default duplicated | **Done** | code | The string lived in both `config/theme.php` and `HandleInertiaRequests.php`, so editing the config alone changed nothing. Now one source |
| Header's only filled button sells logistics | **Done** | code + setting | Admin → Menu shipped `Track order` as the *placeholder*, nudging the owner to spend the store's one primary button on logistics. Placeholder changed, the default is now **Shop gifts**, and the field is explained. *Correction: the live setting already reads “Shop Gift” — an earlier note here said “Track Orders”, read off a LiteSpeed-cached page rather than the database. Checked directly on the server since.* |
| Emoji as the icon system | **Done** | code | ~45 emoji literals across the React storefront replaced with the stroke icon set (`Icons.jsx` extended from 19 to 34 marks). The trust-badge and feature-strip settings now take an **icon name** from a picker instead of demanding an emoji, and a migration remapped the emoji already stored in the live settings — unrecognised glyphs are left alone rather than guessed, and still render. `IconSetParityTest` fails the build if the PHP picker list drifts from the JS set. **Not swept:** the three emoji in the legacy Blade trust strip (`resources/js/app.js`), which dies with the dead-Blade deletion in step 07 |
| Palette roles muddled | **Partial** | code | Semantic `success` / `danger` / `warning` / `promo` tokens added, warmed toward the gold, and **all 136** stock Tailwind red/violet/green/amber classes across 19 storefront files swept onto them — so statuses now follow a palette change instead of clashing with it. **Remaining:** the announcement bar and member bar still carry their own literal `#161618`/`#f5edda` (which happen to equal ink-900/gold-100, so they look right but will not follow a palette swap), and `card_border_color` is a sixth colour. Both need a “follow brand colours” option in Appearance rather than a silent default change |
| Favicon + app icon | **Done** | code | `public/favicon.ico` was a **0-byte file**; the admin layout ignored the uploaded favicon; the manifest emitted one `sizes:"any"` icon and hardcoded the gold palette. All fixed |
| Brand story / About page | **Done** | code | A `brand_story` landing template already shipped but had no URL and nothing linked to it. `/about` now routes, and the footer links it |
| "Cash on delivery / nationwide" repeated 6x | **Partial** | code + content | The four hardcoded repeats in `Home.jsx` (hero trust line, gift-finder blurb, the three gift promises, the four gifting steps) are now `home_content` settings, so they can be edited at all — previously they could not. **Writing the real copy is yours** — Appearance → Homepage content |
| Naming: Ring/Rings, Jewellery/Jewelry | **Todo** | content | Pure content. Renaming a category is safe — the model only generates a slug when blank, so `/category/ring` keeps working and no redirects are needed |

---

## Step 03 — Start collecting reviews

**Roadmap:** *"Ask after delivery."* — **the machinery is almost entirely built.**
*(dossier stale: it read as though this were unbuilt)*

| Item | Status | Owner | What remains |
|---|---|---|---|
| Customer review submission | **Done** | code | Form and endpoint exist, rate-limited 5/10min |
| Points awarded for an approved review | **Done** | code | The reward the dossier said "already exists" — confirmed |
| Rating aggregation + star display | **Done** | code | The all-five-gold-stars bug was the missing theme shades; fixed in `0fb357a` |
| Admin moderation | **Done** | code | pending / approved / hidden |
| Verified-buyer flag | **Done** | code | It *is* set from the order — `ReviewController::store()` matches the typed phone against the orders table. The new invite page prefills that phone, so a review arriving from our own request lands verified. Confirmed end to end against the database (`7d7fa1f`) |
| **Post-delivery review request** | **Done (code) / Todo (setting)** | code + setting | `reviews:request` runs daily, finds orders the courier confirmed delivered, waits out a delay and queues `SendReviewRequest` — SMS plus email where there is an address — carrying a signed `order.review` link that works for a guest. Delivery time is read from `order_status_history`, so pre-existing orders need no backfill. **Off until you switch it on** in Admin → Notifications: it spends SMS credit. Three brakes: a delay window, a max-age window so the first run does not text every historical order, and a per-run cap. `php artisan reviews:request --dry` lists who would be asked (`7d7fa1f`) |

---

## Step 04 — Make gifts findable

*(dossier partly stale — the price filter it asked for already exists)*

| Item | Status | Owner | What remains |
|---|---|---|---|
| Price/budget filtering | **Done** | code | `price_min` / `price_max` / `price_range[]` are honoured *(dossier stale)* |
| Gift finder by budget | **Done** | code | Built, linked, honoured by the catalog (`7daefff` un-hid it) |
| Products can carry tags today | **Done** | code | Admin form, quick-edit, CSV import, **and** the MCP/API layer can all set tags |
| Budget inputs in the catalog UI | **Partial** | code | `price_min`/`price_max` work but are URL-only — no min/max box in the sidebar. A budget *collection* is now the better answer for the common bands |
| In-stock / on-sale toggles | **Partial** | code | Configurable and query-honoured, but never rendered |
| Tags sidebar group | **Partial** | setting | Works as a URL param; the sidebar group is off by default |
| Search misses tags, description, category | **Done** | code | `scopeSearch` now covers tags, the long description and the category name. `gift` returns products again |
| **`q` is silently dropped on category pages** | **Done** | code | `category()` now calls `->search()` like the other two catalog routes. Regression test pins both the count and `searchQuery` |
| Occasion concept on products | **Done** | code | **Collections.** A saved rule set (tag / price / category / stock / on-sale / colour / flags, matched all-or-any) with its own page, image, SEO and menu slot. Smart collections keep themselves up to date; manual ones are a picked list; a smart one can also pin products on top |
| Occasion tiles link to unfiltered `/shop` | **Done (code) / Todo (content)** | code + content | Appearance → occasion tiles now has a “Point this tile at a collection” picker. **Building the collections and tagging products is yours** — the tiles still land on `/shop` until you do |
| Bulk "tag selected products" | **Done** | code | Add / Remove / Replace in the product list's existing bulk bar, sharing its checkboxes and select-all. Replace refuses an empty box, so there is no way to wipe every tag off 200 products by accident. The writer joins with `", "` — one of the two separators the collection matcher recognises — and a test pins that a tag written here is matched by a live `tag is` rule while still never dragging `gift-card` in behind `gift` (`7d7fa1f`) |

---

## Step 05 — Squeeze the server

| Item | Status | Owner | What remains |
|---|---|---|---|
| Pageview logging off the request path | **Done** | code | Moved to `terminating()` |
| Homepage query caching, N+1 sweep, setting memo | **Done** | code | `ce4c9c7` |
| Cache off the database | **Done** | infra | `DEPLOY.md` and the 2026-08-01 SSH check both record **Redis** in production *(dossier stale — the local `.env` says `database`, but that is the dev environment)* |
| Sessions off the database | **Done** | code + infra | `session:to-redis` copies live sessions across without logging anyone out — the two handlers do not store the same bytes (database base64-encodes, cache stores raw), so a column copy would have corrupted every session silently. Sessions get their own Redis database, because `cache:clear` is a whole-database FLUSHDB and `deploy.sh` runs it every deploy. `SESSION_CONNECTION` is only read under the redis driver, so a rollback cannot 500 the site (`61c9435`) |
| **Synchronous Meta CAPI POST on PDP + checkout** | **Done** | code | ViewContent, InitiateCheckout and AddToCart moved to `terminating()` — the same mechanism the pageview insert already uses — with the browser signals snapshotted eagerly so the payload cannot depend on how much of the request is still standing. Deliberately *not* queued: a live check found ~390 ViewContents in three days, which would swamp the same queue that carries order SMS. Deferred sends cap at 5s because they still hold an lsphp worker; Purchase keeps its full 10s and stays queued, since `send()` never throws and a timeout there is silent permanent loss (`61c9435`) |
| Fonts converted to WOFF2 | **Todo** | code | Long-cache headers shipped in `ce4c9c7`, but **no transcode exists at all** — uploads are stored byte-for-byte. TTF is still accepted with no warning |
| Edge/page cache for guest HTML | **Todo** | infra | Cloudflare page rules or LiteSpeed |

---

## Step 06 — Sharing cards and accessibility

| Item | Status | Owner | What remains |
|---|---|---|---|
| Per-page Open Graph cards | **Done** | code | Every page type emits a card; image resolves category image → page LCP image → shop logo. Verified live on `/` and `/shop` (`77123bd`) |
| Canonical + structured data | **Partial** | code | Canonical present; JSON-LD coverage incomplete |
| Off-screen menus stay tabbable | **Done** | code | Both overlays are `inert` when closed — measured in a browser, the sixteen drawer links can no longer take focus. Open, they behave like the dialogs they claimed to be: focus in, Tab trapped, Escape out, focus back to the trigger. `role="dialog"` moved off the full-screen wrapper, which had been announcing an open modal on every page (`5d60183`) |
| Focus reset after Inertia navigation | **Done** | code | Skip link, focus to `<main>` and an aria-live route announcer (`77123bd`) — *(row was stale)* |
| Icon-only buttons without a name | **Done** | code | Swept the storefront. The cart button announced as a bare number (element content outranks `title`); the product-card Add button had no name at all on phones, where its visible label is `display:none` (`5d60183`) |
| Contrast against WCAG AA | **Done** | code | New `ink-500` token (4.83:1 on white) for struck-through prices and other real text; ~150 low-alpha text classes raised to `/70`, the break-even step for 4.5:1 — `/65` still fails. `ink-400` stays on borders, glyphs and disabled controls, which are exempt (`5d60183`) |
| Skip link, landmarks, heading order | **Done** | code | Footer headings were h3s on h1-only pages, a site-wide skip. Homepage builder blocks with a blank title now emit an `sr-only` h2 rather than inventing visible copy. Every nav landmark named; form labels associated across auth, profile, addresses, tracking and checkout (`5d60183`) |

---

## Step 07 — Then, and only then, the admin panel

| Item | Status | Owner | What remains |
|---|---|---|---|
| Admin as React/Inertia | **Todo** | code | **Zero** admin screens are Inertia today — every one is Blade. This is the ~40-screen job the dossier said to leave until last. Start with the order page |
| Live admin notifications without reload | **Done** | code | The bell polls a JSON feed every 25s (`7daefff`) — the one piece already done |
| ~2,500 lines of dead pre-React Blade | **Todo** | code | Delete after the React storefront has soaked another week |
| Dead "Product page template" setting | **Done** | code | Finished on 2026-08-30. `9e66ef9` removed the picker and the config block but left three references in the categories admin view — `config('theme.product_templates')` resolved to `null` and `@foreach(null)` **500'd Admin → Categories in production** until it was reported. The dropdown, the row badge and the last `product_template` theme default are gone, and `AdminScreensSmokeTest` now walks every parameterless admin route so the next dangling reference fails a test instead of a store owner. The nullable `categories.product_template` column is left in place — inert, and dropping a live column is not a cleanup |
| Homepage still forks Blade vs React | **Partial** | code + setting | React only while the homepage template setting is `couture` |
| Inertia page tests | **Partial** | code | Only a few of the React pages assert component + props |
| Any JS test harness | **Todo** | code | No vitest/jest at all |
| MCP product lifecycle | **Partial** | code | Create / publish / edit / photos / archive / search all work. Variants, offers, category *creation*, orders and customers are deliberately still admin-only |

---

## Loose findings not on the numbered roadmap

| Item | Status | Owner | Note |
|---|---|---|---|
| Push prompt fires 1.5s after landing | **Done** | code | Waits for register or order (`77123bd`) — *(row was stale)* |
| Post-registration push prompt is dead | **Done** | code | **The stated cause was wrong.** The script lives in the root Blade view, which an Inertia visit never re-renders — so the trigger could never be *observed*, whatever the localStorage key did. It arrives as a shared prop now, with one key per trigger so register and order each fire once instead of whichever came first silencing the other (`5d60183`) |
| Checkout captures phone on blur, undisclosed | **Todo (content)** | content | The in-field disclosure shipped in `77123bd` and was **removed on 2026-09-11 at the owner's request**, as part of de-cluttering checkout. Nothing on the storefront now says the number is stored before the order is placed, so the **privacy page is the only place left to say it — and it still describes capture at order time**. That wording is yours to correct |
| Assistant transcripts now stored, undisclosed | **Todo (content)** | content | Chat with the assistant was kept only in the shopper's own `sessionStorage` and the server logged nothing. From 2026-09-12 each exchange is appended to `assistant_conversations` so the owner can read what people ask (Admin → Chat history). Transcripts are pruned after 180 days and no IP address is kept, but **the privacy page does not mention chat at all** — that wording is yours |
| Storefront opt-out could delete the owner's order alerts | **Done** | code | A browser has one push endpoint, so the row registered as a staff device was the same row the shop's "turn off notifications" pointed at — and `Shop\PushController::unsubscribe` deleted it with no audience guard. Scoped to `->customers()`; staff devices are turned off from the admin only. Pinned by a test |
| New-order alerts left no trace | **Done** | code | "I'm not getting alerts" was unanswerable: a push that was never sent looked identical to one delivered to a device nobody was watching. `NotificationService` now records the last alert's outcome (order, time, device count, or why it was skipped) and the Notifications page reads it back beside each device's last **delivered** time |
| Product photos still carry the Meridian Éclat watermark | **Todo** | owner | Several live product images on noychoy.com are watermarked with the previous brand — seen on products 194, 250, 257, 258, 260, 263 and 319 while assigning colours. Re-exporting them is yours |
| Corrected phone numbers never re-captured | **Done** | code | Keys on the canonical number instead of a boolean latch, so a corrected number re-sends; the lead row also stores the canonical form, so it can match the order that eventually arrives (`61c9435`) |
| Public tracker shows staff notes verbatim | **Done** | code | Notes no longer leave the server for the public tracker (`77123bd`) |
| Exact per-variant stock in page props | **Done** | code | Now a boolean. `PlaceOrder` still re-validates the real quantity, so a client that no longer knows it cannot over-order — pinned by a test (`5d60183`) |
| Checkout errors only as a top banner | **Done** | code | Every field renders its own message with `aria-invalid` / `aria-describedby`, and focus jumps to the first invalid field. The banner summarises instead of duplicating, and still lists anything with no input on the page (`61c9435`) |
| Confirmation page ends the conversation | **Done** | code | Says what happens next: we call, the courier delivers on a named window, keep exactly this much ready. **The "ready-made builder" did not exist** — the same three lines of Carbon were pasted in two places and neither knew that inside Dhaka is faster or that nobody delivers on Friday. `App\Support\DeliveryEstimate` is now the one builder, skips non-delivery days, and the four windows are editable in Appearance (they were config-only, so effectively hardcoded). A stale window is dropped rather than quoting a past date (`5d60183`) |
| Account order detail eager-loads unused history | **Done** | code | Load removed; the page renders neither notes nor the status trail (`5d60183`) |
| Zero social proof on 105 products | **Todo** | content | Closes itself once step 03's request fires |
| Storefront served no crawlable HTML | **Done** | code | The React pages returned a JSON blob and an empty div: **zero `<h1>`, zero `<a href>`** on a live product page. A pre-hydration shell now renders the heading, price, copy and link graph inside `#app`, which React clears on mount. `docs/SEO.md` |
| Nothing on the site said "Bangladesh" | **Done** | code | `lang="en-BD"`, `og:locale` `en_BD`, geo tags, and an `OnlineStore` graph carrying BDT, cash on delivery, area served and real courier rates. Titles fall back to "— Price in Bangladesh" |
| Facets and pagination were duplicate content | **Done** | code | Filtered/sorted/searched grids are `noindex, follow` with no canonical; page 2 canonicalises to page 2 instead of page 1 |
| Homepage SEO title silently discarded on save | **Done** | code | The Appearance field wrote to a key the save loop dropped, so the hardcoded fallback always shipped. Registered in `config/home.php`, with a Google-description field beside it |
| Occasion and price-band collections | **Todo** | content | Bridal, Eid, gifts under ৳1,500, Pohela Boishakh. Six weeks of lead time each — a page published the week of Eid ranks for nothing. `docs/SEO.md` §3.4 |
| Empty image alt text across the catalogue | **Todo** | content | Alt text is how Google Images reads a photo, and Images is where jewelry gets found |
| Product videos never reached the Meta catalogue | **Done** | code | 85 of 105 published products have an MP4, and Commerce Manager said "Videos: Missing" for every one — neither `MetaProductMapper` nor the CSV feed had a video field at all. Both send them now. Player links (YouTube/Vimeo) are dropped, because Meta downloads the file rather than embedding a page |
| Every catalogue sync reported success without reaching Meta | **Done** | code | `items_batch` names an item by `data.id`; we sent a sibling `retailer_id`. Meta answers **HTTP 200** with `validation_status: "Can not find required field id"` and drops the batch, and `decode()` only raises on `error` — so every sync since this engine shipped logged *success* and changed nothing. The catalogue was really being filled by the scheduled CSV feed. `MetaGraphClient::itemsBatch` now translates to `data.id` and raises on a rejection or a handle-less reply |
| Google product category blank on all 19 categories | **Done** | owner | The code reads `categories.google_category`, walks up to the parent, then falls back to a store-wide default. Filled in since: the live `/feed/google.xml` resolves a category for all 147 items, across five taxonomy nodes (191 Earrings ×63, 194 Necklaces ×60, 188 Jewelry ×11, 189 ×7, 192 ×6). Re-verified 2026-09-08 against the deployed feed rather than the database |

---

## Changelog

| Date | Commit | What |
|---|---|---|
| 2026-09-12 | colour filter back in the sidebar | **The colour chip strip is gone.** It had moved the colour facet out of the filter sidebar and onto a row of swatch chips above the product grid; the owner wanted the grid clean, so colour is a checkbox list in the sidebar again — alphabetical, beside Category and Price, exactly as it was. `Catalog.jsx` and `StorefrontFilters::buildGroups()` are byte-identical to their pre-strip form apart from two things kept on purpose: the `colors` key in `hasActiveFilters` (without it, picking a colour and nothing else never showed "Clear all" — a bug that predates the strip), and the `Turquoise` / `Rose` / `Magenta` swatch hexes, which were drawing blank circles beside their checkboxes. The per-colour product counts the strip needed are no longer sent. `ColourSlicerTest` is replaced by `ColourFilterTest`, which pins the sidebar behaviour: alphabetical order, one row per colour, the chosen one ticked, and the grid actually narrowing. 1080 tests |
| 2026-09-12 | five asks | **Order amendment searches, colours become a slicer, chat history exists, and the push alert can finally be audited.** **Adding a product to an order** was a `<select>` carrying all 110 published products *with every variable product filtered out* — "she also wants the ring in size 8" could not be recorded at all. It is a typeahead now (`admin/orders/product-search`, name / SKU / product ID, 12 results with thumbnail and price), and a variable product brings its variations with it: the option is picked in the same breath, the line stores `variant_id` + `attributes` + the variant SKU, and stock comes off the variant. A half-picked line disables the save button **and** is refused server-side, and a variation belonging to a different product is ignored rather than trusted. **Colours**: migration `2026_09_12_100000` folds the spelling drift that fragmented the filter (`Golden`→Gold, `orange`→Orange, `silver`→Silver, case-insensitively) and fills the eight products that had no colour at all. Those eight were decided **from their photographs, not their names** — which matters, because "925 Sterling Silver Opal" is photographed on a gold band and a name-derived rule would have filed it under Silver where no shopper would find it. Metal tone always, plus a stone colour only where the stones are not the clear CZ most of the catalogue uses; a colour the owner has already set is never overwritten. `Turquoise`, `Rose` and `Magenta` were drawing blank swatches and now have hexes. **The colour facet was briefly lifted out of the sidebar** into a row of clickable chips above the grid; the owner asked for the sidebar back the same day — the page reads cleaner without it — so colour is a checkbox list of swatches again, alphabetical, alongside every other group. What survived that revert is the fix underneath it: `colors` joined the list `hasActiveFilters` checks, so picking a colour and nothing else finally shows "Clear all". **Chat history** (`admin/conversations`): the assistant kept nothing server-side, so the store could not see a single question it had been asked. Each exchange is appended to its own table — two tables, not a JSON blob, because the browser only ever sends the last few turns and a long chat has to stay whole. The list carries the opening question, whether the assistant failed, and a search that looks *inside* what was said; the transcript page shows both sides plus the product cards a reply put in front of her. Pruned after 180 days, and the whole capture path swallows its own errors so a logging failure can never reach the customer. **Push**: it was not broken. Order 10022's alert reached FCM at 17:33:11, one second after the SMS, and the one subscribed staff device's `last_used_at` moved with it — there is simply only ONE device registered, a Windows desktop Chrome, and no phone. Two real defects were found and fixed around that: the storefront's "turn off notifications" deleted staff devices (one endpoint per browser, no audience guard), and an alert left no trace anywhere, so "I'm not getting alerts" could not be told apart from "it went to a device I wasn't looking at". The Notifications card now names the last alert, its device count or the reason it was skipped, and each device's last **delivered** time. 1079 tests (up from 1038). |
| 2026-09-11 | checkout de-clutter | **Checkout stripped to the shortest honest path to a COD order.** One column instead of two, and the **Place order** button now sits four fields in — right after Area / Thana — with everything that is not needed to get there below it: the delivery-zone picker, the gift card, the order note, and the order summary itself. The summary is a fold, shut on arrival, its header carrying the item count and the total; the button carries the total too, because nobody should have to open anything to see what they are agreeing to pay. Removed: the *Want a surprise on your special day?* birthday/anniversary capture (the profile page still collects it, and `checkout.store` still accepts the fields, so the occasions automation is untouched), the *Free delivery unlocked* badge, the four trust badges under the button, the *Have an account? Log in* line above the form (the member box below the summary stays), and the phone field's *We save this so we can call you…* note. The site footer menu is gone from this page only — `<Layout minimalFooter>` renders a copyright line and nothing else, so the last thing under a half-filled address form is not twelve ways to leave. `trustBadges` and `isMember` are no longer sent to the page. **Two things worth knowing:** the zone picker now sits *below* the button, so an address that does not say "Dhaka" will be charged the outside rate unless the customer scrolls down and says otherwise — the auto-detect from the typed address is doing more work than before; and the removed phone note was the code-side fix for the on-blur capture disclosure, which now rests entirely on the privacy page (see the loose-findings row). 1033 tests |
| 2026-09-10 | collapsed description | **The product description starts collapsed.** The descriptions run long — several paragraphs under Design / Craftsmanship / Stones headings — and open by default they pushed the Details table, the reviews and the bundle far below the fold on a phone. It is now a tap to open, and the copy stays in the DOM behind a `hidden` class either way, so search engines still index it and nothing has to be fetched on the tap. The earlier comment argued the opposite (a shopper wants the material before paying a courier); the owner has seen the live page on a phone and called it, so the reasoning is recorded rather than silently reversed. No test pins the state — it is client-side React state, not server-rendered. Verified in the browser: `aria-expanded` starts `false`, one click reveals the copy. 1038 tests. **Declined again, third asking:** fabricating five Banglish reviews per product under invented Bangladeshi women's names, and randomising “Loved by” to 50–100. Fabricated reviews and inflated counts are deceptive to real shoppers making a spend decision and carry consumer-protection exposure; the store already has a real `Review` model and a review-invite page, so the honest route is the post-delivery request automation in step 03. |
| 2026-09-10 | bangla feature strip | **The trust wording the owner actually meant was still in English.** The de-clutter pass translated `theme.trust_badges` — which render on the product page, checkout and footer — but the strip sitting high on the homepage under the hero is a *different* setting, `home_content.feature_strip`, and it still read “Fastest Shipping Countrywide / Easy Return Policy / Premium Quality Product”. That is the first trust wording most shoppers see, so translating the badges alone undid most of the point. Bangla defaults now (সারা দেশে দ্রুত ডেলিভারি / সহজ রিটার্ন পলিসি / প্রিমিয়াম কোয়ালিটি পণ্য / ২৪/৭ অনলাইন সাপোর্ট), with migration `2026_09_10_120000` rewriting the stored English rows in place the way `…100300` does for the badges — same normalise-then-map shape, same rule that anything the owner wrote themselves is left alone. **Checked against production before shipping** rather than assumed: the live `theme.trust_badges` holds all five retired English defaults verbatim (so all four translate and the 7-day row drops) and `home_content.feature_strip` holds three of the four retired strip defaults, so both migrations land. `home_content.hero_trust` has no stored row at all, which makes `…100200` a no-op there and the config default the thing that shows. Also: “Picked for you” lost its heading entirely at the owner's call — `CardSection` takes a `bare` flag that drops the eyebrow and title, and shows the browse link at every width since with no heading there is nowhere else for it to live. The server-side `pickedForYou.title` prop is untouched, so `PersonalRowsTest` still pins it. `tests/Feature/FeatureStripBanglaMigrationTest.php`, 5 cases. Verified against a copy of the dev database: the strip renders Bangla after migrating. 1038 tests |
| 2026-09-10 | storefront de-clutter | **The product page, homepage and header lost the clutter the owner flagged from a phone.** Product page: the gallery is now a swipeable scroll-snap track — a finger swipe moves through every photo *and* video (videos sit behind a poster and play on tap, so an iframe never eats the swipe), with a “n / total” pill, desktop paddles, arrow keys and the thumbnails still in sync with variant picks. Gone from the buy box: the Member pill and its “Join free” line (moved onto the cart nudge and the checkout member box as a shared `MemberPill`), the “Arrives … / Dispatching …” box, the Care and Shipping & returns accordions (their admin fields and `pdp_care_text`/`pdp_returns_text` keys went with them), the reward-ladder “Add more, save more…” line and the duplicate “Cash on delivery available” beside the quantity stepper. The reward strip under the header is gone too — from the React header and from the Blade layout that landing pages and the legacy home templates still use — so rewards live in the cart page and the mini-cart only; the shared `ladder` prop stays because both still read it. Trust badges are now Bangla by default (ক্যাশ অন ডেলিভারি / সারা বাংলাদেশে দ্রুত ডেলিভারি / ন্যায্য দাম / অথেনটিক কোয়ালিটি গ্যারান্টি) and the 7-day badge is retired to keep the list short (the refund page still carries the window); because the live store has the old English rows *saved*, migration `2026_09_10_100300` rewrites exactly those rows in place and leaves any owner-written badge alone. Homepage: “Gift message included” dropped from the hero trust line (default + migration `2026_09_10_100200` for the stored copy), the For her / For him recipient chips removed from the gift finder (occasion chips stay — they filter), “Picked for you” no longer carries the “— for him, birthday…” suffix, “Browse Our Categories” moved directly under the reassurance strip, and the “Gifting, made effortless” band is **off** behind a new Appearance toggle (`show_gifting_steps`, default false) so the owner can switch it back on. **Two asks were declined on purpose:** generating fake Banglish reviews under invented names and inflating “Loved by” to a random 50–100. Both are fabricated social proof shown to real shoppers, so they were not built. In their place the product page hides the five empty stars until a real review exists and shows “Loved by N people” only from 10 real loves; the honest route to reviews is the post-delivery request automation in step 03. Also: `config/inertia.php` published with the page path spelled `js/Pages`, because Inertia’s lowercase default made every `assertInertia()` fail on a case-sensitive disk. Verified in headless Chromium at phone width (swipe, video, cart, checkout, home order). 1033 tests |
| 2026-09-10 | order status slicer | **The orders list filters by status without going through the dropdown.** A `<select>` can only ever say one thing at a time, so the owner's actual day — bouncing between Pending, Processing and Booked — was three round trips through it, and “Pending *and* Processing together” was not expressible at all. The list now carries a row of pills above the search box: **Pending / Processing / Booked with courier** by default, each with a live count, and an **All** pill. Which three are pinned is the owner's call — **✎ Edit pills** opens a picker over the nine statuses (capped at three, since a fourth wraps the row) and saves store-wide. A **Multi** switch changes what a click means: off, a pill replaces the filter the way the dropdown does; on, pills add up, and the dropdown beside them reads “2 statuses selected” rather than lying about showing everything. The filter itself now takes a set (`?status=pending,processing`, or the array form), drops keys that are no longer real statuses instead of rendering an empty table, and still opens on Processing when nothing is asked for. Pill counts follow the search box and the Trash view but deliberately ignore the active status, so the row stays readable once you use it. The pills are plain links — Alpine only swaps the href while Multi is on. Caught on the way: aliasing the count query as `total` collided with `Order`'s own `decimal:2` cast and printed “Processing 2.00”. `tests/Feature/AdminOrderStatusSlicerTest.php`, 22 cases. Two neighbours fixed while in there: `?q[]=` was a 500 on the orders list, and the “parcels never booked” alert counted processing *and* booked while linking to processing alone — a link the old single-value filter could not have expressed. 1028 tests |
| 2026-09-10 | system config | **Test connection works, and saving no longer asks for a password.** The button never reached its own route. It overrides the form with `formaction`, and `admin-ajax.js` read only the form's `action` — so every click posted to **save**, where it died on the save form's password rule and looked like a button that did nothing. The ajax layer now takes `formaction` / `formmethod` / `formtarget` from the clicked button, and passes the submitter to `FormData` so a named submit button's own value survives too. The same bug had the Meta panel's Test Connection button posting to save; it is fixed by the same line. **The password prompt is gone** from save, restore and import: `system-config.access` (Super Admin) is the decision about who may edit configuration, and re-typing the signed-in account's own password proved nothing on top of it while adding a step to every edit. Every change is still versioned, audited and auto-backed-up first, so an unwanted edit is visible and reversible. `ConfirmsAdminPassword` and `RestoreConfigRequest` are deleted; the Meta panel's separate unlock password is untouched. One thing the working button exposed: a test re-rendered the section and **emptied everything just typed**, which would have made checking a value cost you the value. The form comes back holding it now — and a rejected save does the same — except secrets, which are never echoed into the UI, so the test result says outright that a typed key has to be entered again. Field keys contain dots, so `old('values')` is indexed by hand; `old('values.google.ads_id')` looks for a nested array that does not exist. `tests/Feature/SystemConfigSectionScreenTest.php`, 8 cases, plus two more in `AdminFormActionClobberTest` pinning the submitter. Restore's policy check rode in on the deleted form request, so it is asserted in the controller now rather than left to the admin middleware alone. 1006 tests |
| 2026-09-09 | google tag | **The storefront reports to Google Ads.** There was no Google tag on noychoy.com at all — the GA4 field in System Config accepted a value, the connection tester validated it, and nothing was ever written onto a page. Now: gtag.js sitewide (deferred like the Pixel), GA4 + Ads conversion IDs, enhanced conversions with SHA-256 email/phone hashed server-side, and the `google-site-verification` tag Merchant Center needs. The whole funnel is wired without touching a single React file — `partials/google-tag` decorates the `window.track()` the Pixel already defines, so ViewContent/AddToCart/InitiateCheckout/Purchase/Search each emit their Google twin. `item_id` is `meta_content_id()`, the same string the product feed sends as `g:id`, which is what makes dynamic remarketing able to show the exact piece someone viewed. A purchase sends twice on purpose: `purchase` to GA4 with per-line items, `conversion` to `AW-…/label` for bidding — one event to both would either double-count or record nothing. **The CSP blocked all of it**: `script-src` allowed only facebook, and the tag was written and nearly shipped before a browser said so. Nine Google endpoints are now allowed, each one observed in a real load — including `google.com.bd`, because conversions ping the visitor's country domain. A malformed ID is dropped rather than emitted, since gtag accepts one silently and simply records nothing. `tests/Feature/GoogleTagTest.php`, 15 cases. 990 tests |
| 2026-09-09 | abandoned checkout follow-up | **The abandoned-cart screen became a follow-up desk.** It held a phone number, a list of product names and one "mark contacted" button; everything else the shopper typed at checkout was thrown away unless the order completed, so recovering a sale meant asking for the address again on the call, and nobody could tell who had already been rung. Capture now keeps the **address, area and delivery zone** (`LeadController`, plus `onBlur` on those two fields in `Checkout.jsx` — every captured field is folded into the dedupe key, or a later value never sends) and the **`visitor_token`**, the one column that makes a lead's traffic source knowable: visits key on that cookie, carts on the session id, and until now no row carried both. A blank field is skipped rather than written, so the name-blur capture cannot wipe the address a later one learned — mutation-tested, because the first version of that test passed with the guard deleted. New **lead page**: the cart with photos, variants and a badge on any line the restore link will drop, the address, whether the number has ordered before (`CustomerInsight`) and how those parcels went, where she came from, and a **follow-up log** (call / WhatsApp / SMS / note, outcome, who and when) replacing the single boolean. **Reaching her** is the point: Call, WhatsApp and Send-SMS-now all carry the signed `cart.restore` link that rebuilds her exact basket — the WhatsApp wording is editable in Settings → Integrations and the link is appended even if the owner rewords it away. Bulk select on the list for SMS / mark contacted / delete, with a 24-hour skip so a second click does not buy the same message twice. **The trap worth knowing:** `abandoned_carts.updated_at` is the SMS runner's entire due window, so every admin-side status write goes through `AbandonedCart::stampQuietly()` — a plain `save()` when marking a lead contacted would make a three-week-old cart look freshly abandoned and text her out of nowhere. Sidebar badge and a deep-linked dashboard alert. `tests/Feature/AbandonedCartFollowUpTest.php`, 24 cases. **Follow-up fix same day:** deleting a lead 404'd. Two causes, both about a URL outliving the row it names — bulk delete used `back()`, and `back()` is the last page the browser actually *asked the server for*, so leaving a lead page via the browser's own Back button left the deleted lead as the redirect target; and a lead URL is cached in the notification bell and bookmarked, so it kept 404ing after the row was gone. Bulk delete now falls back to the list when "back" is one of the rows it just deleted, and every `{cart}` route has a `->missing()` handler that says the lead is gone instead of showing a bare 404. 993 tests — the jump from the 975 quoted above is the 15 cases `GoogleTagTest` brought with the Google Ads commit that landed in between, not a change here. **Second follow-up fix:** the 404 was still there, and neither earlier cause was it. The production access log named it exactly — `POST /admin/[object%20HTMLInputElement] 404`. A form exposes its own fields as properties and a field *shadows* the attribute it collides with, so the bulk bar's `<input name="action">` made `form.action` return that input rather than the URL; `admin-ajax.js` did `fetch(form.action)`, the browser stringified the element, and all three bulk buttons posted into nowhere. Server-side tests all passed because the request never reached the server. Fixed at both ends: the field is `bulk_action` (so even a stale cached bundle works), and the ajax layer reads `getAttribute` for `action`/`method`/`target`/`download` instead of the clobberable properties. `AdminFormActionClobberTest` fails if any intercepted admin POST form grows such a field again — `products/index` is listed as a known-safe exception because it submits via `form.submit()`, which the ajax layer never sees. 995 tests |
| 2026-09-09 | feed descriptions | **Meta gets the same flattened copy Google does.** Descriptions are authored in the light-markdown the storefront renders, and all three listing surfaces print the field verbatim — so `## Design` and `**Turquoise & Rose**` reached shoppers on Facebook and Instagram exactly as they had on Google. `plain_copy()` learned links and code ticks (which also fixes the SEO shell, meta descriptions and schema.org, its other three callers), and a new `feed_description()` wraps it with bullets, blank-line collapsing and a per-channel length cap. `/feed/meta.csv`, `/feed/google.xml` and `MetaProductMapper` all call it, so one product reads the same wherever it is listed. **This moves the payload hash the sync engine de-duplicates on, so every product re-pushes on the next Sync all rather than being skipped** — the same one-off cost the video change had. `tests/Feature/FeedDescriptionTest.php`, 9 cases, one per surface. 952 tests |
| 2026-09-09 | catalogue sync was a no-op | **Every Meta catalogue sync this store has run reported success without touching the catalogue.** Found while chasing "Videos: Missing": `items_batch` identifies an item by `data.id` — and `id` there means the *retailer* id — but the client sent it as a sibling `retailer_id` next to `method`. Meta does not treat that as an error you can see. It replies **HTTP 200** with `{"validation_status":[{"errors":[{"message":"Can not find required field id"}]}]}` and discards the batch, while `MetaGraphClient::decode()` only raises on an `error` key. So `syncProduct`, `syncChunk` and every **Sync all** wrote `status = success` to `meta_sync_logs`, stamped `meta_sync_states` as synced, and sent nothing. What actually kept the catalogue current was the hourly scheduled CSV feed at `/feed/meta.csv` — 147 items, matching the catalogue's own `product_count`. `itemsBatch()` now owns the wire format: it moves the retailer id to `data.id` (so nothing above it has to know Meta renames it on this one endpoint) and raises `MetaApiException::VALIDATION` on either a `validation_status` rejection or a 200 carrying no handles — non-retryable, because re-sending the same malformed payload fails identically. Verified live against catalogue `4371156476430134`: the old shape draws the rejection, the corrected shape returns a handle, and pushing `prod-181` moved its `video_fetch_status` from `NO_STATUS` to `OUTDATED` — Meta accepting the video and fetching the file. Because the stored payload hashes record pushes that never happened, the first run after this must be **Full catalog refresh**, not Sync all — run on deploy: 105 products, 3 batches, 0 failures, and a `success` in `meta_sync_logs` now means Meta actually took it. End state audited across all 147 catalogue items: `category` set on **147/147** (194 ×60, 191 ×63, 188 ×11, 189 ×7, 192 ×6) and `video_fetch_status` **FETCHED on 98** — exactly the 98 feed rows that carry a clip, with the other 49 having no video to fetch. One trap for whoever reads this next: the field is written as `google_product_category` but **read back as `category`**, so a check that asks for the name it was written under gets an empty answer and looks like a failure. `tests/Feature/MetaItemsBatchWireFormatTest.php`. 943 tests |
| 2026-09-08 | google feed | **The catalogue now has a Google Shopping feed.** Merchant Center's "Add products from a file" data source fetches `https://noychoy.com/feed/google.xml` on a schedule — no Google Cloud project, service account, or re-push cron, which is what the Merchant API route would have cost. Not the Meta CSV renamed: Google rejects rather than ignores the differences, so the feed sends `in_stock`/`out_of_stock` (underscored), a `g:shipping` block carrying the admin's inside-Dhaka rate, and `identifier_exists: no` — without that last one Google demands a GTIN and disapproves every item, since nothing in this catalogue has one and the SKU is our own code, not an MPN. Item ids are `meta_content_id()`, the same string the Meta feed and the Pixel/CAPI use. Variants become one item each under `item_group_id`. Products priced 0 are dropped: Google rejects them, and shipping one would advertise a giveaway. Both feeds now share `publishedProducts()`, so the draft-leak fix stays fixed in one place. Descriptions are flattened out of Markdown first — they are authored in it and the storefront renders it, but Google prints the field verbatim, so all 147 items would have reached shoppers with `##` and `**` still attached. `tests/Feature/GoogleFeedTest.php`, 16 cases. 943 tests |
| 2026-09-08 | catalogue videos | **The product videos now reach Meta.** 85 of the 105 published products carry an MP4, and Commerce Manager reported "Videos: Missing" on every single one — not a Meta problem, a gap on our side: neither the live sync path (`MetaProductMapper`, which feeds catalogue `4371156476430134` over `items_batch`) nor the CSV feed at `/feed/meta.csv` had a video field at all, so Meta was never told. Both now send `video` / `video[N].url` from `Product::galleryVideos()`, absolute and direct-to-file. YouTube and Vimeo entries are filtered out rather than sent: Meta downloads and re-hosts the file, so a player page is rejected item-by-item. Variants repeat the parent's clip — the video shows the piece, not the size. An empty gallery omits the key entirely, because Meta reads `"video": []` as "delete the videos on this item". Adding the field moves the payload hash the sync engine de-duplicates on, so the 85 already-"synced" products re-push on the next **Sync all** instead of being skipped. `tests/Feature/MetaCatalogVideoTest.php`, plus three feed cases. 921 tests |
| 2026-09-08 | assistant takes orders | **The chat assistant can take a cash-on-delivery order end to end.** The customer pastes a product link or names the piece; the assistant asks the checkout questions, reads the whole summary back, and places the order once she agrees. `App\Services\Ai\ChatOrder` + four tools (`choose_item`, `set_order_details`, `review_order`, `place_order`), none of which accepts a price — every figure is computed by a second cart (`CartService::scoped('chat')`) through the same cascade the cart page uses, so a chat order costs what the website charges. Guards: the second cart never touches her real basket; a `quote_id` hash means only the exact order she was quoted can be placed; that quote must have been issued on an *earlier* message, so the model cannot quote and order in one breath; the order number is written into the draft, so a repeated call returns the same order rather than a second parcel. Ceiling on order value, per-customer daily cap and an off switch in System Config → AI assistant. Chat orders are stamped `source = 'chat'`. `tests/Feature/AssistantOrderTest.php` |
| 2026-09-08 | phone header + assistant voice | **Phone-only header changes and a professional assistant.** Strip on phones: one Bangla line ("প্রতি পিসেই রিওয়ার্ড") beside the badge plus rungs 1–5 · … · last; desktop keeps the live message. Every rung carries its value underneath on every screen (৳50, 2%, Delivery, …), in equal-width cells so captions of different widths do not set the gaps. A centred "৳X saved" badge in the phone header opens the cart. That figure is `CartService::totalSaved()` — money off the pieces **plus** the delivery charge once free delivery is unlocked, valued at the lower zone rate (৳80 inside Dhaka against ৳90 outside) so it is never overstated; `discount()` is untouched, because delivery is its own line on the order. The cart's Delivery row reads "Free — ৳80+ saved" and the mini-cart's free-delivery row shows "−৳80+". Removing an item (and changing a quantity) no longer re-mounts the cart page: the row goes on tap and the request runs behind it with `preserveState` — React and Blade, seeded server-side (`cart.saved_text` shared prop, `window.__cartSaved`). Assistant: Sir/Ma'am only, Bangla script for Bangla *and* Banglish questions, an explicit cannot-do list (no "I'll WhatsApp the team"), wa.me link given with the number and rendered tappable; transcript validation accepts the assistant's own long turns (a 1,244-char reply had been failing every later message with a 422); OpenAI failures log at error level. Meta Pixel JS deferred to after load / first touch. Owner-visible perf facts: origin is Hetzner Falkenstein (DE), app time ~40 ms, TTFB from Bangladesh ~400 ms is network; home images served as 1600px originals where 293px tiles are rendered |
| 2026-09-08 | personal store | **The ladder is always in view, the assistant speaks first, and the store remembers people.** Header reward strip on every page (React `LadderStrip` + a Blade twin), message for the next rung, rungs auto-fit the width (mobile: 1–5 · … · last). Assistant: "How can I help you today?" opener with the customer's first name, a once-per-session "Kichu lagbe? 👋" teaser beside the launcher, language-aware starter chips, product cards no longer crushed by the flex log. Occasions: birthday/anniversary captured at checkout and in the profile, `crm:occasions` (09:30 Dhaka) sends a reminder N days before and a wish on the day (bell + push + SMS, optional % offer), Admin → Offers card. Tier progress ("৳X from Gold") in cart/mini-cart; abandoned-cart SMS/push name the next rung (`LadderLine`). Assistant `my_orders` tool for members. Referral programme (`/invite/{code}`, cookie, points to both sides on delivery, reversed on return; Account → Invite friends). Recently viewed + "Picked for you" home rows from the gift finder's answers (`GiftProfile`, `Recommendations`). Language preference (`Locale`: cookie + member record, footer toggle, set by writing Bangla to the assistant) drives Bangla chrome copy. Meta: `value` sanitised (>0, 2 dp) on Pixel and CAPI; admin Test panel refuses without a Test Event Code and no longer fires the browser Pixel — the cause of Meta's "all Purchase events send the same price" |
| 2026-09-07 | rewards + assistant | **Ten-rung reward ladder, membership made visible, a chat assistant, and filled collections.** `GiftLadder` rewritten as a cumulative cart ladder (৳50 off at 1 piece, 2% at 2, free delivery at 3, ৳60/65/70/70/80 at 4–8, a free gift at 9, ৳100 at 10; rungs editable in Admin → Offers, defaults in `config/ladder.php`, tier + rewards persisted on the order). `chrome.membership` now feeds a home-page band, header label, cart/mini-cart/checkout/register/login/footer nudges. `App\Services\Ai\AssistantService` + `/assistant/chat` + `partials/ai-chat`: an OpenAI (gpt-5-mini) assistant, Bangla/English, with live product search and a phone-verified order lookup; key and switch under System Config → AI assistant. All 105 products tagged by occasion so the Anniversary / Birthday / Date Night / Gift for Her smart collections fill (50 / 55 / 69 / 105). Support address is support@noychoy.com everywhere |
| 2026-09-07 | domain move | **The store is `noychoy.com` now, and it is called NoyChoy.** cPanel addon domain repointed at `public/` on `alt-php83`; `APP_URL`/`APP_NAME` and the admin System Config updated; every stored `meridianeclat.shop` link (menu, home content, discover tiles, theme, SMS templates, content templates) rewritten; store name, footer, SEO title, policy pages and SMS wording say NoyChoy; `meridianeclat.shop` and `www.` 301-redirect via `public/.htaccess` (webhooks and `.well-known` exempt). Old wordmark cleared, placeholder "N" favicon shipped. Owner rows 9–12 above are the follow-ups |
| 2026-08-22 | `0fb357a` | Stars, floats, zone default, CSRF, privacy |
| 2026-08-22 | `ce4c9c7` | Bundle split, LCP preload, font caching |
| 2026-08-22 | `7daefff` | Gift-card settings, free-delivery checkout, member nudge, live admin bell |
| 2026-08-22 | `9de7081` | Steps 01 and 02 closed on the code side; roadmap re-audited. 508 tests (was 485) |
| 2026-08-22 | *this change* | Step 04: Collections — Shopify-style smart product groups, in the menu, the sitemap and the occasion tiles. 538 tests |
| 2026-08-23 | `77123bd` | Sharing cards on every page, keyboard/screen-reader pass, two privacy fixes. 541 tests |
| 2026-08-23 | `61c9435` | Checkout badge + field errors, Meta CAPI off the render path, sessions ready for Redis. 556 tests |
| 2026-08-23 | `7d7fa1f` | Step 03 post-delivery review requests; step 04 bulk product tagging. 580 tests |
| 2026-08-23 | `5d60183` | Step 06 keyboard access + contrast; confirmation page delivery estimate and COD-ready line. 592 tests |
| 2026-08-23 | `51a470a` | **New audit, 91 verified findings.** Money leaks: coupon overcharge, points destroyed on cancel, Meta AddToCart blind outside the PDP, UTC vs Dhaka delivery dates, review-request backlog burn, guest cart-recovery SMS, cart delivery cost, real co-purchase suggestions. 604 tests |
| 2026-08-23 | `3497553` | Admin: correct a delivery address, take an order by hand, partial deliveries, integration health alerts, Reviews 500 on a deleted product, Steadfast off the page-load path. 615 tests |
| 2026-08-23 | `9a7aad3` | Landing-page icon names printed as text; Meta token + 1,200 phone numbers purged from production logs; admin role gate and the first negative authz tests. 631 tests |
| 2026-08-23 | `f833695` | Speed: brand fonts actually served (146 KB, not 268 KB force-preloaded), filter sidebar cached with versioned busting, Deals of the Day cached, related products hydrate only the winners. 638 tests |
| 2026-08-23 | `15671a9` | Search rebuilt: multi-word, relevance-ordered, typo correction (transposition-aware), and a zero-result page that recovers instead of dead-ending. 652 tests |
| 2026-08-23 | `8b73b45` | Order items can be added and removed (with the stock deltas that were never applied); COD payment recorded on delivery. 661 tests |
| 2026-08-23 | `f01d7c7` | Responsive images via srcset + a mid-size variant; guests offered an account on the confirmation page, gated by proof the order is theirs. 668 tests |
| 2026-08-23 | `9e66ef9` | 2,724 lines of dead Blade deleted, after single-sourcing the homepage React/Blade fork that would have silently reverted the live homepage. Product-template picker removed |
| 2026-08-23 | `6d25c4d` | Admin forms submit in the background and swap the changed region — no full-page reload. 672 tests |
| 2026-08-30 | *this change* | **The review SMS fits one segment.** It was 299 characters — two paid segments per send — and 119 of those were the signed link's 64-character signature. A short door onto the same page (`/r/{order}/{token}`, a 64-bit HMAC checked with `hash_equals`, throttled) verifies and redirects to the signed URL, so `invite()`'s three-way allow list is untouched; email keeps the full link. Wording trimmed to store, first name, order, link, 10%, code, expiry — first name only, applied to this message alone via the extras override. **142 characters on all three live queued orders, one segment, every character GSM-7**, with tests pinning both the 160 ceiling and the alphabet (one em dash would flip the gateway to UCS-2, where a segment is 70). 750 tests |
| 2026-08-30 | *this change* | **Two broken admin panels, and the traffic report rebuilt.** **Broadcast SMS 500'd after charging for it**: a bulk send goes to the gateway as one comma-joined string, and `sms_logs.phone` is a `varchar(255)` — 100 numbers is ~1,400 characters, so the INSERT overflowed, the catch block wrote the same oversized row again, and the second failure escaped as a 500. The first hundred customers had already been messaged; the other 536 were never reached and nothing was logged. The log write can no longer throw, bulk rows store a summary plus a new `recipients` count, and the send is queued in chunks (`SendBroadcastSms`) so it cannot hit `max_execution_time` and a failed chunk retries alone. **Every product link on the dashboard 404'd**: `Product::getRouteKeyName()` is `slug`, and *viewed but never bought*, *running out soon* and *dead stock* all built their links from the id — the slug now travels with the row. **"Other website" was Facebook.** Meta's `{{site_source_name}}` macro resolves to `fb` / `ig` / `an` / `msg`; `TrafficSource` only matched full platform names, so every tagged ad click fell through to `referral` — 83 of the day's 179 visitors. Short names now resolve, an ad id in the URL marks a click paid without a `utm_medium`, and `visits:reclassify` re-files the history from the referrer host. Visits and orders also keep `utm_medium`, `utm_content` and the ad id, so the new **Ads & campaigns** table can name the ad, not just the campaign — and it starts from *visits*, so an ad that sent 400 people and sold nothing is visible instead of absent. The visitors bar chart drew nothing (percentage heights inside an auto-height flex column); it is replaced by a five-line **traffic & conversion over time** chart — visitors, product views, add-to-cart, checkout, orders — with toggles, rescaling and a hover readout, hand-drawn in SVG rather than adding a chart library. New **on the site right now** card polls `admin/dashboard/live` for who is browsing and what they are looking at. 781 tests |
| 2026-08-30 | `cebffbf` | **Post-delivery review requests, with a private thank-you offer.** The review-request automation is switched **on**; each request now also mints a one-use percentage coupon reserved to that buyer's phone (`coupons.reserved_for_phone`) and puts the code in the same SMS and email. New `is_exclusive` coupons do not stack — they are weighed against the running offer/member/personalised discounts (delivery value included) and only the better deal applies, with a cart notice explaining a code that stood down. Adversarial review found **ten** defects, all fixed before deploy: coupons were the last place gift-ladder free units still earned a percentage; the exclusive comparison ignored free delivery and silently dropped it; a suppressed personal offer was still burned; the one-use cap had no row lock (two tabs could spend it twice); a 24-bit code space would have handed one buyer another's code by ~5,000 coupons; the mint sat outside the job's try/catch, which would have silenced that order's request permanently; a re-sent request could advertise an expired code; and private codes appeared top of the broadcast-push picker. **Known limit, by design:** the phone at checkout is unverified, so the reservation stops a shared or posted code from being spent by strangers, not someone given both the code and her number — closing that needs an OTP or login this COD store deliberately does not have. 740 tests |
| 2026-08-30 | `20f4426` | **Two reported bugs.** Admin → Categories was 500ing on every load — the deleted product-template picker left three `config('theme.product_templates')` references in its view, and `@foreach(null)` is fatal; the dead markup is gone and a smoke test now renders every parameterless admin screen (the missing coverage that let it reach production). The homepage flashed plain text before the real page: that is the pre-hydration SEO shell React clears on mount, so it is now held at `opacity:0` for script-capable visitors and reveals itself after 1.2s only if the bundle never arrives — crawlers and JS-less visitors still get it verbatim. 718 tests |
| 2026-08-30 | `37c1fd7` `2aca77b` | **The .com look, on .shop.** PDP rebuilt to mirror meridianeclat.com: vertical trust-badge list (now up to 6 on the PDP; footer keeps 3), beige arrival box with a dispatch date (`DeliveryEstimate::dispatch`), Quantity label, light-markdown description renderer (`## `/`- `/`**`), a Details spec table from custom fields, and admin-editable Care + Shipping & returns accordions. **Milestone gift ladder** (every Nth piece free from an admin-picked collection, Shopify-BxGy math, cheapest free first, server-side only) with cart/mini-cart progress bar — shipped **OFF**; enable in Admin → Offers once the two collections are built. Refund policy default moved to a 7-day change-of-mind window (schema `return_days` 7). Perf: built font CSS was double-emitting @font-face so browsers picked WOFF over WOFF2 (post-build merge fixes it), deals cards ship 450px variants instead of 1600px originals, FBT/story images downsized, `image_variant` stat memo, homepage reviews band cached, unused highlight-categories query dropped from the React homepage. `catalog:export-copy` / `catalog:apply-copy` commands for the full-catalog description+specs rewrite. 714 tests |
| 2026-08-26 | `4c02eda` | **SEO, rebuilt for Bangladesh.** The React storefront was serving crawlers an empty div; it now ships a pre-hydration HTML shell with the heading, price, copy and link graph. Market targeting (`en-BD`, geo, BDT, COD, courier rates) in an `OnlineStore` + `WebSite` + `Product` graph; facets and pagination stop duplicating; one shared SEO head replaces two drifted copies; product images enter the sitemap; the Appearance SEO fields stop being thrown away. Full audit and the owner's half in `docs/SEO.md`. 695 tests |
| 2026-09-01 | *this change* | **Any “%” in an SMS silently killed the whole send.** The gateway was called as `GET /sendtext?…&messageContent=…`, and it decodes its query string *twice* — a correctly-escaped `20%25 OFF` came back as a stray `% O`, the parse of the entire query string collapsed, and `toUser` was lost with it. The gateway then blamed the number: `-55 Destination ID Empty`, on a number that worked with the word “Hi”. Every promotional broadcast quoting a discount was rejected in full — nothing sent, nobody reached, and the error pointed at the wrong field. Probed live against the gateway with an unallocated destination: `%` fails, `É`, newlines and length are all fine, and a form body passes `%`, `&`, `+`, `#`, `=` and Bengali verbatim. The send is now a POST with the fields in the body — what KhudeBarta's own WordPress plugin does. Tests pin the method and that nothing rides in the query string. 812 tests |

---

## What shipped in this change

Roadmap steps **01** and **02**, everything on them that is not the owner's
decision to make.

**Step 01 — the free-delivery promise.** The threshold was env-only, so the
store owner could not set it without SSH, and the banner was arbitrary free
text with no connection to it. `free_shipping_threshold()` is now the single
place the number is resolved; the cart, the checkout, the order total and the
banner all read it. It is a field in Admin → Settings beside the shipping
rates, and blank means off. A `{free_delivery}` placeholder prints the live
amount, and the message is hidden entirely while the promise is off — so the
bar cannot advertise something the checkout will not honour. **What is
deliberately not done: choosing the number.** That is a margin decision.

**Step 02 — the brand leaks.** Repo-level `Noychoy` defaults corrected; the
duplicated `footer_about` default collapsed to one source; the header CTA
placeholder no longer nudges toward order tracking and a commercial label
defaults in; the emoji icon system replaced by the storefront's own stroke
icons, including a migration for emoji already saved in the live settings; 136
stock Tailwind status colours swept onto brand-warmed semantic tokens; a real
favicon shipped (the repo's was a **0-byte file**) and the manifest fixed to
emit installable 192/512 maskable icons and follow the store palette; `/about`
added so the brand-story template that already shipped finally has a URL and a
footer link; and the homepage's hardcoded "cash on delivery" repeats made
editable.

### Collections (step 04)

A **collection** is a saved rule set with its own page, image, SEO and menu slot
— the thing the roadmap needed for "make gifts findable", and the answer to
occasion tiles that dead-end on the full catalogue.

- **Rules**: tag, title, SKU, description, colour, price, compare-at price,
  stock, weight, category, in-stock, on-sale, featured, bestseller, pre-order,
  has-options, date added — matched **all** or **any**. The vocabulary lives in
  one place (`App\Support\CollectionRules`); the admin dropdowns, the query
  compiler and the validation all read it, so adding a rule is a one-file change.
- **Smart or manual**, mirroring `CustomerSegment`. A smart collection can also
  pin products, which show on top of whatever the rules match.
- **Empty means empty.** A smart collection with no usable rules returns
  nothing, deliberately: showing the whole catalogue under a name like "Eid
  Gifts" looks like it worked when it did not.
- **Whole-tag matching.** `tag is gift` does not match `gift-card`. The
  catalogue stores both `Gift` and `gift`, so matching is case-insensitive.
- **Live preview** in the admin: the match count updates as you build the rules,
  before anything is saved, and it tells you how many rows it had to ignore.
- **Menu**: a menu item, a dropdown child or a mega-menu link can point at a
  collection by name instead of a typed URL. Resolved once at save time —
  `site_menu()` runs on every request, so resolving there would be a query per
  item per page. Renaming a collection keeps its URL, so nav links never break.
- Collections are in the **sitemap** (cache key bumped so a warm cache cannot
  keep serving the old one) and are SPA-navigable.

Three bugs fixed in the same pass, all confirmed against the code first:

1. `/category/{slug}?q=` **silently dropped the search term** while the page
   still rendered a search-results state and fired a Meta Pixel `Search` event.
2. Storefront search **missed tags and the long description**, which is why
   searching "gift" returned nothing.
3. `/feed/meta.csv?category=x` **leaked draft products to Meta**: an
   `orWhereHas` outside a closure compiled to `(published AND pivot) OR primary`.
   Also fixed a latent `$type` crash in the menu sanitiser from the same
   `??`-only-guards-the-check pattern.

**Verified:** 508 tests passing, assets rebuilt and committed, and the promise
chain checked in a browser in both directions — ৳5,550 unlocks free delivery
and replaces the zone picker, and with the promise off the picker returns,
shipping is ৳130 and the banner stops mentioning it. No console errors.
